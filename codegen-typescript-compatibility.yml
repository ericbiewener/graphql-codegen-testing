overwrite: true
schema: "schema.graphql"
documents: "*.ts"
generates:
  generated-typescript-compatibility.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-compatibility"
    config:
      skipTypename: true
      noNamespaces: true
      # preResolveTypes: true
